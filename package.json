{
  "name": "sprint-3-task-manager",
  "version": "1.0.0",
  "description": "Sprint 3 Task Manager assessment with update, LocalStorage and unit tests",
  "type": "module",
  "scripts": {
    "test": "jest --env=jsdom",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@jest/globals": "^30.1.3",
    "jest": "^30.1.3",
    "jest-environment-jsdom": "^30.1.3"
  }
}
